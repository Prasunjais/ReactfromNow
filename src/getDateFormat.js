import dateFns from "date-fns-tz";
import moment from "moment-timezone";
export const getDate = () => {
  // return moment(1536484369695).fromNow();
  // return dateFns.formatDistance(new Date(1536484369695), new Date(), {
  //   addSuffix: true
  // });
  // return new Intl.RelativeTimeFormat().format(-4, "day");
};
